#include <stdio.h>

int main() {
    char ch;

    // Prompt the user for input
    printf("Enter a character: ");
    
    // Read a single character input
    scanf("%c", &ch);
    
    // Print the character
    printf("You entered: %c\n", ch);

    return 0;
}
